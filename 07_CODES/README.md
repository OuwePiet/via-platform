# VIA – SQL / Database code

Deze map is gereserveerd voor de gecontroleerde databasecode van VIA.

## Verwacht hoofdbestand

`sql_migratie_compleet.sql`

Dit bestand wordt pas aangemaakt wanneer de volledige SQL-definitie of voldoende gecontroleerd bronmateriaal beschikbaar is. We verzinnen geen tabelkolommen, constraints, RLS-policies, indexes of triggers.

Zie voor de reeds bevestigde databasearchitectuur:

`docs/G_Database_SQL.md`
